const statesEnabledNotification = {
  TRANSIT: 'TRANSIT',
  DELIVERED_CLT: 'DELIVERED_CLT',
  RETURNED: 'RETURNED',
  BLOCKED: 'BLOCKED',
  CANCELLED: 'CANCELLED',
  REGISTERED: 'REGISTERED',
};

const sourceChannelEnabled = { B2B: 'B2B' };

module.exports = {
  statesEnabledNotification,
  sourceChannelEnabled,
};