{
  "name": "update-discount-image-batch-service",
  "version": "1.0.5",
  "description": "It goes through the products that do not have an image and looks in the aws-s3 service if there is an image to associate and if it exists it updates the discount in the database.",
  "main": "index.js",
  "dependencies": {
    "b2b-aws-s3": "^1.0.0",
    "b2b-response": "^1.0.0",
    "b2b-repositories": "^1.0.0",
    "b2b-aws-configs": "^1.0.0",
    "b2b-managers": "^1.0.0",
    "aws-sdk": "^2.799.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "dotenv": "^8.2.0",
    "jest": "^26.6.2"
  },
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}

